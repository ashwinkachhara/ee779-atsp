load('submarine.mat');

dl = d/lambda;

phi1 = beamform(X, 128, dl);
figure(1)
plot(phi1)

phi2 = capon_sp(X, 128, dl);
figure(2)
plot(phi2)

theta1 = root_music_doa(X, 2, dl);
theta2 = esprit_doa(X, 2,
